- hosts: keycloak
  remote_user: centos
  tags: all
  tasks: []

- hosts: keycloak
  remote_user: centos
  become: true
  tasks:
    - debug: var=hostvars[inventory_hostname]['ansible_ens3']['ipv4']['address']
  roles:
    - ipaclient
    - keycloak
